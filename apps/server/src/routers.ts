// Placeholder - API router will be implemented in Phase 2
// Structure from technical architecture §4.1:
// - session (me, listMemberships)
// - org (create, get, listMembers, invite, setMemberRole)
// - sites (list, get, create, update, setBillingConfig, delete)
// - devices, meters, readings, demand, dataGaps, tariffs, invoices, reconciliation, alerts, fleet

export const appRouter = {
  // Placeholder
};
